language: c
compiler: gcc-4.8

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - gcc-4.8

before_script:
  - alias gcc='gcc-4.8'
  - alias cc='gcc-4.8'
  - cc --version
  - cd src/

script: make && make test